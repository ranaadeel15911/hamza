import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["drive.google.com"], // Add your allowed domains here
  },
  // Add other Next.js configuration options here
};

export default nextConfig;
